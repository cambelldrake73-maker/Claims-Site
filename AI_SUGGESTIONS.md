- Implement a Centralized Schema Registry microservice with versioning, compatibility checks, access controls, and pub/sub rollout (standalone implementation not already completed)
- Build a Token Revocation Service with immediate distributed blacklist propagation and admin revoke endpoints for JWT invalidation (distinct implementation not present in memory)
- Create a Per-Field Access Control middleware service that enforces field-level RBAC at the service/data-access layer with policy decision caching and immutable evaluation logs (separate from general Data Access Governance)
- Develop a Consent & Data Access Authorization microservice to manage clinic consents, time-limited PHI access scopes, revocations, and full audit trails (new standalone implementation)
- Integrate an external Policy Engine service (OPA/Rego) with policy lifecycle management for dynamic authorization rules used by submissions, exports, and sensitive actions (not implemented)
- Design and implement end-to-end encrypted job payload handling for the job queue system where payloads are envelope-encrypted and decrypted only by authorized worker instances with key rotation and worker attestation (distinct implementation)
- Build a Claim Replay & Incident Debugging service that can re-run claims through parsing/normalization/correction pipelines in an isolated sandbox with versioned schemas and replay controls (not implemented)
- Create a Centralized Niche Router Authorization Adapter that enforces niche-specific RBAC and consent checks before routing uploads into healthcare pipelines (new adapter)
- Design and implement a Versioned Schema Migration Service that applies and validates schema changes across the Centralized Schema Registry and downstream services (not present)
- Implement an Automated Data Quality & Schema Drift Detection pipeline: nightly profiling, anomaly detection on canonical claim fields, and alerting for schema drift (new system)
- Build a Claim Normalization microservice that transforms parsed claims into the canonical schema with per-field provenance, validation hooks, and migration tooling (standalone implementation)
- Implement a Denial Knowledgebase API with canonical denial code taxonomy, versioning, crosswalks (NCPDP/CMS/CARCs/RARC), source provenance, and an authenticated lookup service (not present)
- Develop a Correction Suggestion Engine service that uses deterministic rule sets + ML model scoring to propose claim corrections with explainable reasons and provenance (new microservice)
- Create an ML Model Serving platform for recoverability scoring and denial-to-correction ranking, with model versioning, A/B rollout, and explainability hooks (not implemented)
- Build a Parser Router microservice that selects pluggable parsers based on file type and schema, with idempotency, validation, and structured error reporting (new service)
- Implement a Pluggable EDI/X12/835/277 Parser library as a secured service with schema extraction and mapping to canonical schema (not implemented)
- Design a Claim Enrichment service that augments claims with external payer/provider/patient data via secure connectors and caches, with connector adapters and rate-limit handling (new)
- Implement a Claim Lineage & Provenance Service that records per-field source, transformations, and chain-of-custody for every claim (distinct from event store)
- Build a Clearinghouse Adapter Framework that supports multiple clearinghouse adapters (sandbox/prod), transactional submission semantics, idempotency keys, and per-adapter health checks (new implementation)
- Create an EDI 837 Formatter microservice with templating, strict segment/loop validation, automated test harness, and dry-run mode for reviewer validation (standalone service)
- Implement a Submission Orchestrator that manages submission workflows, transactional retries, DLQ routing, and submission-level audit trails for clearinghouse sends (not present)
- Develop a Secure Document Access Broker that issues short-lived, scoped tokens for PHI documents, enforces masking rules, and logs per-request audit entries (distinct from existing document proxy)
- Build End-to-End Distributed Tracing integration across all services (OpenTelemetry), including trace correlation for job flows, per
