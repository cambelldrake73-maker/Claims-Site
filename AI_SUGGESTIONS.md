- Design and implement a HIPAA-compliant backend platform: define a microservice architecture (API gateway, claim-processing service, appeals/workflow service, notifications, secure document proxy, billing service) with an authentication & authorization stack (OIDC + RBAC), centralized immutable audit logging, envelope encryption (TLS in transit + KMS-wrapped at-rest), idempotent APIs, durable async processing (message queue/streaming), API versioning, and a secure document access proxy that surfaces protected files without modifying them.

- Define and build a canonical data and analytics layer: formalize a normalized OLTP data model for patients/claims/transactions with immutable change-history (audit trails or event sourcing) to track claim lifecycle; implement a separate analytical data warehouse (columnar store) fed by CDC/ETL pipelines for denial-rate, aging, and recovered-revenue analytics; include data governance, lineage, and HIPAA-safe synthetic test data generation for analytics and QA
