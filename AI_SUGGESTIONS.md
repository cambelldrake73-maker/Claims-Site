- Implement a hardened API Gateway and edge security layer (managed API Gateway/Kong/Traefik) that centralizes auth enforcement (JWT + RBAC), per-tenant rate limiting and quotas, schema-based request validation, WAF rules, TLS termination, CDN caching for public assets, and secure proxy routing for protected document access (signed-URL passthrough) — include comprehensive ingress/egress logging and metrics hooks for downstream observability without touching protected file contents.  
- Build a production-grade, event-driven claims processing pipeline to replace the “Redis planned” gap: deploy a durable message broker (Kafka or managed SQS+SNS), implement the transactional outbox pattern for DB→event consistency, durable worker pools with idempotency keys, exponential backoff & dead-letter queues, replay capability for audits, and operational tooling for scaling, monitoring, and tracing claim lifecycle events — integrate with the existing background job architecture and ensure end-to-end error handling and SLA-driven retries.  
- Deliver a full GitOps CI/CD and infrastructure-as-code platform for safe, auditable deployments and compliance: Terraform/CloudFormation modules for infra, pipeline templates (GitHub Actions/GitLab CI) enforcing unit/integration/contract/e2e tests, DB migration orchestration with pre/post checks and safe migration strategies (canary
