- Design and implement a scalable Claim Parsing & Normalization microservice (worker-based) that ingests claim files from the frontend/event-bus, performs deterministic OCR/field-extraction and schema validation, emits normalized claim, patient, provider and line-item records to a versioned event stream, and supports retries, backoff, DLQ and auditable events for downstream consumers.
- Build a Denial Intelligence Platform with a unified denial-code taxonomy and feature store, streaming/batch pipelines to compute denial rates and root-cause classifications, model training and evaluation pipelines for appeal-success and recovery-amount predictions, model monitoring/CI and an API to surface ranked remediation recommendations to the review UI.
- Implement a Secure Clearinghouse Gateway service that performs EDI 837/277 translation and validation, supports sandbox and production endpoints, enforces idempotent submission/reconciliation flows with retry/DLQ, strong auth/transport security, signed transmissions, per-transaction audit logs, and operational monitoring/alerting for failed exchanges.
