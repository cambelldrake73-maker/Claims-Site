- Create services/claim_ingestion/claim_deduplication with a persistence model (models/deduplication.ts) and a Redis-backed dedupe worker (workers/dedup_worker.ts) that checks canonical unique keys and emits DeduplicationResult events to the job queue.
- Add services/denial_intelligence/denial_reason_classifier scaffold with an API: classifyDenial(denialText: string): Promise<{code:string, confidence:number}> and include a local rule-based fallback using payer rule config files under services/denial_intelligence/payer_rules/{payerId}.yaml.
- Build services/correction_suggestion_engine module: add an interface suggestCorrections(normalizedClaim: NormalizedClaim, denialClassification) -> SuggestionSet, plus a persistence model suggestions/{claimId}.json and a worker workers/suggestion_worker.ts that enqueues suggestions into the review_workflow queue.
- Create services/review_workflow_service: add an approval_queue persistence schema (Postgres) for review tasks, an API route POST /review-queue to enqueue human review tasks, and a worker workers/review_timeout_worker.ts that escalates stale reviews after configurable TTL.
- Implement services/clearinghouse_adapter_framework with adapters/adapter_interface.ts defining submitClaim(formattedEdi: string): Promise<SubmissionResult>, add a mock/soap_adapter.ts and a tests/adapter-integration.test.ts, and wire edi_formatter output to call clearinghouse_adapter_framework.adapter_interface in a new submission orchestrator services/clearinghouse_submission_orchestrator/orchestrator.ts.
