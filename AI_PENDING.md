- Create services/denial_intelligence/denial_knowledgebase module with data folder denial_codes.yaml and a loader that exposes getDenialMetadata(code): {description, severity, recoverabilityHints}; add services/denial_intelligence/denial_classifier.ts that provides classifyDenial(normalizedClaim) returning matched codes and candidate corrections.
- Add services/correction_suggestion_engine with suggestions/suggester.ts implementing a deterministic rule runner: accepts normalizedClaim + denialClassifier output and returns an array of suggestedCorrections (field, suggestedValue, rationale, confidence); include a config file rules/default-correction-rules.json and an adapter to enqueue suggestions into review queue.
- Implement services/review_workflow_service: add API routes POST /review/assign, GET /review/queue, PATCH /review/{id}/approve and a queue model review_queue.ts (Postgres table sql/migrations/202603_review_queue.sql) that stores suggestion payloads, assignee, status, timestamps, and audit fields; add a worker module workers/review_worker.ts that claims review jobs and records audit log entries via audit_log_service API.
- Create services/edi_formatter adapter interface adapters/edi_formatter/interface.ts defining formatTo837(normalizedClaim, approvedCorrections): {ediString, metadata}, and add a reference implementation adapters/edi_formatter/hl7x12_formatter.ts that maps canonical schema fields into an EDI 837 skeleton and returns validation errors; add unit tests and an API endpoint POST /submission/format that uses the interface.
