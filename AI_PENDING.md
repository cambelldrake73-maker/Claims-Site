- Add canonical_claim_schema/schema v1 JSON Schema file under schemas/canonical_claim_schema/v1.json and implement a TypeScript validator module schemas/validators/canonicalValidator.ts that exports validateCanonicalClaim(obj): ValidationResult using AJV; include sample normalized claim fixture.
- Implement claim_normalization module services/claim_normalization with normalizeClaim(rawClaim, schema): NormalizedClaim; add mapping rules file services/claim_normalization/mappings/edi-to-canonical.json and a normalization worker normalizeWorker.ts that accepts parser_router output messages.
- Create services/claim_deduplication folder and add a persistence model (db/models/claim_dedup.ts) with a deterministic fingerprinting function (fingerprintClaim) and a datastore interface (dedupRepository.ts) plus a deduplication worker that marks duplicates in a claims table.
- Add denial_intelligence_engine/denial_reason_classifier directory with an initial rules-based classifier implementation (classifier.ts) that consumes normalized claims and denial codes, outputs {reasonCode, confidence, suggestedCorrectionIds}; include a JSON denial code knowledgebase data file denial_knowledgebase.json.
- Create correction_suggestion_engine service scaffold services/correction_suggestion_engine with an Engine class that accepts classifier output and returns suggestion drafts; include interfaces for SuggestionModel, an adapter to persist suggestions (persistence.ts), and an API contract file api/contracts/correctionSuggestion.v1.json.
- Add clearinghouse_adapter_framework/adapters/baseAdapter.ts and implement a mock clearinghouse adapter services/clearinghouse_adapter_framework/adapters/mockAdapter.ts that implements submitClaim(normalizedClaim): Promise<SubmissionReceipt> and a connector registry to register adapters by payer ID.
- Wire parser_router output into claim_normalization entrypoint: add integration code services/integrations/parser-to-normalizer.ts which subscribes to parser_router events (or queue topic parser.parsed) and enqueues normalize tasks onto the job_queue (jobs/queueModels/normalizeClaimJob.ts) with a sample worker implementation jobs/workers/normalizeClaimWorker.ts.
