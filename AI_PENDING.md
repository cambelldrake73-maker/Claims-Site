- Create services/denial_intelligence_engine folder with scaffold and add denial_code_knowledgebase JSON (sectors/healthcare/code_sets/denial_codes.json) plus a REST route POST /classify that returns denial_reason_code, confidence, and recoverability_score; include contract tests and mock ML model adapter interface libs/ml/modelAdapter.ts.
- Add correction_suggestion_engine module services/correction_suggestion_engine/suggester.ts that consumes enriched normalized claims, applies rule templates (sectors/healthcare/rules/correction_rules.yaml), produces suggested corrections (SuggestedCorrection model), persists suggestions in suggestions table (migration SQL), and emits review task to queue review-approval.
- Wire parser_router output into claim_normalization in integration: add orchestration glue services/orchestrator/ingest-orchestrator.ts which receives parse results, validates against canonical_claim_schema, calls claim_normalization.normalize, and on success enqueues normalization-complete event; include OpenAPI route POST /orchestrate/ingest and integration tests.
