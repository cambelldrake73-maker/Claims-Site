- Create services/claim_deduplication module: add deduplication service (dedupe.ts) that computes a deterministic dedup_hash from canonical claim fields, exposes a function to check/merge duplicates, and a route /dedupe/check for synchronous dedupe checks used by claim_normalization worker.
- Wire parser_router -> claim_normalization -> claim_deduplication -> recoverability_scoring_model pipeline: add orchestration code (orchestrator.ts) in services/ingestion_orchestrator that subscribes to parser_router parse-job events, enqueues normalization jobs, calls dedupe check before persisting, and invokes recoverability scoring to attach score metadata; include unit-test stubs for each handoff.
