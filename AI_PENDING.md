- Add services/claim_ingestion/schema_registry/schemas/canonical_claim_schema.json and implement a JSON Schema validator module services/claim_ingestion/schema_registry/validator.js that validates incoming parser output against the canonical_claim_schema and returns canonicalization hints.
- Create persistence model services/claim_lifecycle/models/claim_event_store.sql (Postgres) and ORM model services/claim_lifecycle/models/claimEvent.js (Sequelize/TypeORM) with fields for claim_id, event_type, payload (JSONB), source_job_id, created_at, and lineage_parent_id.
- Add services/denial_intelligence/denial_knowledgebase folder with denial_codes.yaml, an index.js loader that exposes getDenialMeta(code) and a CLI script to import payer-specific code-sets into the knowledgebase table (services/denial_intelligence/db/migrations).
- Build services/correction_engine/correction_suggestion_engine scaffold with worker.js that subscribes to normalized_claims, calls denial_knowledgebase.getDenialMeta, runs rule-based suggestion pipeline (rules/*.js), and enqueues suggestion jobs to review_queue via job_queue API.
- Create services/review_workflow/approval_queue_service: add API routes POST /api/review/queue (enqueue suggestion), GET /api/review/queue?status= (list), and persistence schema services/review_workflow/models/reviewQueueItem.sql with states (pending, in_review, approved, rejected) and audit fields for reviewer_id and decision_timestamp.
