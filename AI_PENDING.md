- Add parser_router module services/parser_router/index.js with plugin registry, implement interface functions registerParser(niche, parserFactory) and route(parsedBundle) and add unit tests; include a parser plugin shim for healthcare that returns normalized raw claim JSON (no file edits).
- Add canonical_claim_schema JSON Schema files under schemas/canonical_claim/v1/schema.json and schemas/canonical_claim/v1/claims-entities.json and implement AJV validators in libs/validators/canonicalClaimValidator.js used by claim_normalization entrypoint.
- Add services/claim_enrichment module with an enrichment worker (workers/claim_enrichment_worker.js) that subscribes to the job_queue topic claim:normalized, looks up payer, CPT/ICD code metadata from data/code_sets/healthcare, enriches records, and writes back enriched_claims table schema in db/models/enriched_claims.sql.
- Create services/denial_intelligence_engine folder with two components: denial_knowledgebase (data/denial_codes/ccs.json) and denial_reason_classifier API scaffold (POST /classify) that accepts enriched claim JSON and returns top-3 coded denial reasons with confidence; include model interface file models/classifier_interface.js for future ML model wiring.
- Implement services/correction_suggestion_engine with an interface suggestCorrections(enrichedClaim) in services/correction_suggestion_engine/engine.js, a rules config file rules/correction_rules.yaml, and a persistence table correction_suggestions (db/models/correction_suggestions.sql) storing suggested fixes and source rule IDs for audit.
